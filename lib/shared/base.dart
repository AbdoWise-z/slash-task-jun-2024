
/// the url to the deployed API
const String API_LINK = "api.jsonbin.io";

/// API request time out
const Duration API_TIMEOUT = Duration(seconds: 5);

/// weather or not to enable the mock API (load from assets)
const mockAPI = false;